# TASK-001A

`src/components/cases/MasterCaseTemplate.tsx` foi reconstruído integralmente, preservando `MasterCaseTemplate` e suas props `item` e `journalHref`.

Nenhum outro arquivo foi alterado. A abertura do VitruChat agora usa o asset v2-light solicitado, breadcrumb e retorno para Home.
