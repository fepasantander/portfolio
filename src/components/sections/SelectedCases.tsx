import Image from "next/image";
import Link from "next/link";
import { Container } from "../ui/Container";
import { publishedPortfolioCases } from "@/data/case-production-pipeline";

export default function SelectedCases() { return <section id="cases" aria-labelledby="cases-title" className="py-28"><Container><p className="text-xs font-medium uppercase tracking-[0.2em] text-cyan-600">Selected work</p><h2 id="cases-title" className="mt-3 text-4xl font-semibold tracking-tight sm:text-5xl">Cases</h2><div className="mt-12 grid gap-8 md:grid-cols-2">{publishedPortfolioCases.map((item) => <article key={item.slug} className="group overflow-hidden rounded-xl border border-zinc-200 bg-white shadow-sm transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lg dark:border-zinc-800 dark:bg-zinc-950"><div className="relative aspect-[16/9] overflow-hidden"><Image src={item.heroImage.src} alt={item.heroImage.alt} fill className="object-cover transition-transform duration-300 group-hover:scale-[1.02]" /></div><div className="border-t-2 border-transparent p-7 transition-colors duration-200 group-hover:border-cyan-500"><p className="text-xs uppercase tracking-wider text-zinc-500">{item.category} · {item.company}</p><h3 className="mt-3 text-2xl font-medium tracking-tight">{item.title}</h3><p className="mt-3 text-zinc-600 dark:text-zinc-400">{item.summary}</p><p className="mt-5 text-sm text-zinc-500">{item.contribution.slice(0, 2).join(" · ")}</p><Link href={`/cases/${item.slug}`} className="mt-6 inline-flex font-medium text-cyan-700 underline-offset-4 hover:underline dark:text-cyan-300">Ver case <span aria-hidden="true" className="ml-1 transition-transform group-hover:translate-x-1">→</span></Link></div></article>)}</div></Container></section>; }
